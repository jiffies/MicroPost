<% if signed_in? %>
	<div class="row">
		<aside class="span4">
			<section>
				<%= render 'shared/user_info' %>
			</section>
			<section>
				<%= render 'shared/stats' %>
			</section>
			<section>
				<%= render 'shared/micropost_form' %>
			</section>
		</aside>
		 <div class="span8">
      <h3>全部扯淡</h3>
      <%= render 'shared/feed' %>
    </div>
</div>
<% else %>
<div class="center hero-unit">
	<h1>欢迎来到扯淡团！</h1>
<h2>
		这是一个让你尽情扯淡的地方=><%= link_to "扯淡助理", user_path(1) %>
  </h2>
<%= link_to "现在就注册吧!", signup_path,class: "btn btn-large btn-primary" %>
</div>
<% end %>
